## v1.2.0 · 长文本修复

修掉「试听长文本只读开头一句」的问题。三个互相独立的真因：

1. **采样器没有重复惩罚**（最关键）：`cosyvoice_ras` 的管线写死为 `range → topK → topP → select`，
   不含 `penalty` 步，而 `repetition_penalty` 只在 `penalty` 步生效 —— 这套 LLM 此前从未有过重复惩罚。
   改为 `mixed` + 显式 `penalty` + `repetition_penalty=1.15`。同一句 92 字从「6 次尝试全部撞满上限、
   无 EOS」变成「首次尝试即正常出 EOS，成品 23.56 s」。
2. **`maxTokens` 写死 500**（≈ 只够 20 秒语音），而 LM 生成的是「音色前缀文本 + 正文」整段。
   改为按字长估算 `(前缀字数+正文字数)*8+128`，钳在 `[500,1024]`。
3. **质量门判据误杀**：旧判据 `longestRun >= 8 即拒` 会把「有 EOS 的正常输出」也拒掉，
   于是 7 次尝试无一通过、整条链直接报错。新判据只拒「没有 EOS」「unique<=4 且 >=20」
   「单段连续重复 >=64」，并新增尾部空转裁剪。

### 顺带修的两处

- 取消「≤24 字走 NPU、否则走 OpenCL」的分流：那个结论的两条依据（`continuousHexagon` 字段、
  `longestRun>=8` 判据）经查都不成立。实测同一句 63 字在 hexagon 上 **4/4 首次尝试即通过**，
  prefill 0.62~0.73 s（OpenCL 要 10.8~22.9 s）。
- 每一次尝试（含 CPU 兜底）都过质量门，不再静默交付退化音频。

### 注册用参考文本 26 字 → 15 字

注册时音频会被实时版上限（125 token = 5.0 秒）截断，而 `promptPrefix` 仍是完整文本，
导致文本与音频提示不对齐。新默认 15 字实测设计出 **4.08 秒 / 102 token，不再被截断**。

### 实测（SM8850）

| 场景 | 结果 |
|---|---|
| 试听 · 短文本 | LLM wall 3.05 s / 67.2 tok/s |
| 试听 · 短文本 | 3.32 s / 58.7 tok/s |
| 试听 · 长文本 | 11.94 s / 47.6 tok/s（231→452 token） |
| 音色创建 | 闭环 97.3 s |

详情见仓库内 `docs/LONG_TEXT_FIX_2026-09-18.md`。
